## 概要
GitHub Actionsを用いたCI環境を構築しました。リポジトリにPushおよびPull Requestが作成された際に、自動で単体テストの実行と、アプリの起動確認が行われるようにしました。

## 変更種類（必須）
- [x] feature
- [ ] fix
- [ ] refactor
- [ ] docs
- [ ] chore

## スコープ宣言（必須）
- 対象: CI環境（GitHub Actions）の新規構築。テスト実行およびアプリ起動確認の自動化。
- 非対象: アプリケーションのビジネスロジックや既存テストケースの修正。

## 変更内容
- `.github/workflows/ci.yml` を新規作成しました。
- `test` ジョブを追加し、Java 17環境で `./gradlew test` を実行するようにしました。
- `run` ジョブを追加し、WireMockコンテナを起動した上で設定ファイルとデータディレクトリを指定して `./gradlew run` による起動確認を行うようにしました。

## 影響範囲
- リポジトリにおけるCIの実行環境（GitHub Actions）。アプリケーションの実行環境や既存コードには影響しません。

## 確認手順
1. 本PRをPushした際に、GitHub Actionsのワークフローがトリガーされることを確認する。
2. Actionsタブから `CI` ワークフローの実行結果を開き、`test` ジョブと `run` ジョブが両方とも正常に完了（Success）していることを確認する。

## 分割方針チェック
- [x] リファクタと機能追加を同一PRに混在させていない
- [x] `domain` 変更時に `infrastructure` を同時変更していない（必要時は別PR）
- [x] `application` にオーケストレーション以外のロジックを追加していない

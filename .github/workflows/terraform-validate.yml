name: Terraform Validate

on:
  pull_request:
    paths:
      - 'infra/terraform/**'
      - '.github/workflows/terraform-validate.yml'
  workflow_dispatch:

jobs:
  validate:
    name: Validate Terraform
    runs-on: ubuntu-latest
    strategy:
      matrix:
        directory:
          - 'infra/terraform/bootstrap/gcp'
          - 'infra/terraform/gcp'

    defaults:
      run:
        working-directory: ${{ matrix.directory }}

    steps:
      - name: リポジトリのチェックアウト
        uses: actions/checkout@v4

      - name: Terraformのセットアップ
        uses: hashicorp/setup-terraform@v3

      - name: Terraform フォーマットチェック
        # Terraformコードのフォーマットが正しいか確認します
        run: terraform fmt -check

      - name: Terraform 初期化 (backendなし)
        # backend=falseで初期化し、GCP認証を回避しつつプロバイダープラグインのみダウンロードします
        run: terraform init -backend=false

      - name: Terraform 検証
        # Terraformコードの構文と構成の有効性を検証します
        run: terraform validate

# AGENTS.md

このリポジトリで作業する Codex 向けのルールです。

## 基本方針
- 回答、説明、PR、README、docs、コメントは日本語で書く。
- コード上のクラス名・関数名・変数名・ファイル名は既存の命名規則に合わせる。
- 既存のディレクトリ構成と設計を尊重する。
- 必要最小限の変更を優先し、段階的に改善する。

## リファクタリング方針
- 既存機能の動作を変えない整理を優先する。
- 勝手に大規模リファクタリングしない。
- 責務分離・重複削減・可読性向上は小さな差分で行う。
- バグ修正時は再発防止テストの追加を検討する。

## 依存関係・設定・運用
- 勝手に依存ライブラリを追加しない。
- 勝手に本番向け設定を変更しない。
- API エンドポイントやレスポンス解釈、CSV/JSON 保存形式を無断で変更しない。
- 秘密情報、API キー、トークン、`.env` をコミットしない。

## 検証と報告
- 変更後は可能な限りテストを実行する。
- テストを実行できない場合は理由を説明する。
- 作業報告では「変更内容 / 変更理由 / 確認内容 / 残っている懸念点」を日本語で明記する。
